[extend]
useDefault = true

[[allowlists]]
description = "Static Cloudflare R2 bucket identifiers in the Render blueprint"
condition = "AND"
paths = ['''^render\.yaml$''']
regexTarget = "line"
regexes = [
  '''^\s*- key: R2_BUCKET_PEAKS\s*$''',
  '''^\s*value: subtitle-fm-peaks\s*$''',
]
